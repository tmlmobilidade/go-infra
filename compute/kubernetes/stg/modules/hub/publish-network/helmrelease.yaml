apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hub-publish-network
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: stg-hub
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: stg
    module: hub
    app: publish-network
    replicas: 1
    resources:
      memory: 4Gi
      cpu: 4000m
    service:
      targetPort: 5050
    ingress:
      enabled: false
    secrets:
      - stg-platform-go-cache-db
      - stg-platform-storage
      - stg-module-hub
      - stg-platform-sentry-hub
    env: []
