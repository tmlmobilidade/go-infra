apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-438-performance-sync-metrics-realtime
  namespace: flux-system
spec:
  interval: 5m
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-438-performance
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    module: performance
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: false
      host: pr-438.go-stg.tmlmobilidade.pt
    secrets:
      - stg-platform-storage
    env: []
    environment: pr-438
    app: sync-metrics-realtime
