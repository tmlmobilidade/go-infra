apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-636-hub-frontend-videowall
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-636-hub
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-636
    module: hub
    app: frontend-videowall
    replicas: 1
    service:
      targetPort: 3000
    ingress:
      enabled: true
      path: /hub/videowall
      pathType: Prefix
      host: pr-636.go-stg.tmlmobilidade.pt
    env: []
  suspend: false
