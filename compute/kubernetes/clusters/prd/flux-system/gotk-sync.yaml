# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  # How often the source should be checked for changes.
  # The default is 1m (1 minute).
  interval: 10s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/tmlmobilidade/go-infra
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  # How often the kustomization should be reconciled.
  # The default is 10m (10 minutes).
  interval: 10s
  path: ./compute/kubernetes/clusters/prd
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
