apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apex-raw-sync-banking-taps
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: prd-apex
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: prd
    module: apex
    app: raw-sync-banking-taps
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: false
    secrets:
      - prd-module-apex
      - prd-platform-godb
      - prd-platform-pcgi-raw
      - prd-platform-clickhouse
      - prd-platform-sentry
    env: []