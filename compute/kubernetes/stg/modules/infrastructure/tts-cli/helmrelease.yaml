apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: infrastructure-tts-cli
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
    - name: infrastructure-tts-server
  targetNamespace: stg-infrastructure
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: stg
    module: infrastructure
    app: tts-cli
    replicas: 1
    resources:
      memory: 4Gi
      cpu: 2000m
    service:
      targetPort: 3000
    ingress:
      enabled: false
    secrets:
      - stg-module-infrastructure
      - stg-platform-godb
      - stg-platform-storage
      - stg-platform-sentry
    env: []