apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stops-tts-cli
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
    - name: stops-tts-server
  targetNamespace: prd-stops
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: prd
    module: stops
    app: tts-cli
    replicas: 1
    resources:
      memory: 4Gi
      cpu: 2000m
    service:
      targetPort: 3000
    ingress:
      enabled: false
    secrets:
      - prd-platform-storage
      - prd-module-stops
      - prd-platform-go-cache-db
      - prd-platform-sentry
    env: []
