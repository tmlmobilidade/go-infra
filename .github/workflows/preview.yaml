name: Create Preview Environment
on:
  repository_dispatch:
    types: [preview-pr]

jobs:
  helm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate Helm release
        run: |
          PR_NUMBER=${{ github.event.client_payload.pr }}
          echo "Creating Helm releases for PR $PR_NUMBER"
          # Generate Helm charts/templates using the PR images