apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-637-plans-validator
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-637-plans
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-637
    module: plans
    app: validator
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: true
      host: pr-637.go-stg.tmlmobilidade.pt
    secrets:
      - stg-module-plans
      - stg-platform-godb
      - stg-platform-email
      - stg-platform-storage
    env: []
  suspend: false
