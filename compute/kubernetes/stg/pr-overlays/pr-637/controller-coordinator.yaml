apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-637-controller-coordinator
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-637-controller
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-637
    module: controller
    app: coordinator
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: true
      host: pr-637.go-stg.tmlmobilidade.pt
    secrets:
      - stg-module-controller
      - stg-platform-godb
    env: []
  suspend: false
