apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-700-operation-gtfs-validator
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-700-operation
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-700
    module: operation
    app: gtfs-validator
    replicas: 1
    service:
      targetPort: 5050
    resources:
      cpu: 6000m
      memory: 12Gi
    ingress:
      enabled: true
      host: pr-700.go-stg.tmlmobilidade.pt
    secrets:
      - stg-platform-godb
      - stg-platform-storage
      - stg-platform-email
      - stg-platform-sentry
    logging:
      enabled: true
      cluster: go-stg
      loki:
        secretName: stg-platform-loki
    env: []
  suspend: false
