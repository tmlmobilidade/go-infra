apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tracker-clickhouse-sync
  namespace: flux-system
spec:
  interval: 5m
  dependsOn:
    - name: onepassword-connect
  targetNamespace: tracker-prd
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    name: tracker-clickhouse-sync
    module: tracker
    namespace: tracker-prd
    replicas: 1
    image:
      repository: ghcr.io/tmlmobilidade/go-tracker-clickhouse-sync
      tag: latest
      pullPolicy: IfNotPresent
    service:
      port: 80
      targetPort: 5050
    ingress:
      enabled: false
    platformSecrets:
      - prd-platform-storage
    env: []
