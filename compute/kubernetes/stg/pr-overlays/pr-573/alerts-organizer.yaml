apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-573-alerts-organizer
  namespace: flux-system
spec:
  interval: 5m
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-573-alerts
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-573
    app: organizer
    module: alerts
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: true
      host: pr-573.go-stg.tmlmobilidade.pt
    secrets:
      - stg-platform-storage
      - stg-module-alerts
      - stg-platform-sentry-alerts
    env: []
  suspend: false
