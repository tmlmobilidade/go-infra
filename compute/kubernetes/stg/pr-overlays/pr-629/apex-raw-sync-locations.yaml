apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-629-apex-raw-sync-locations
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-629-apex
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-629
    module: apex
    app: raw-sync-locations
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: true
      host: pr-629.go-stg.tmlmobilidade.pt
    secrets:
      - stg-module-apex
      - stg-platform-godb
      - stg-platform-labdb
      - stg-platform-rawdb
    env: []
  suspend: false
