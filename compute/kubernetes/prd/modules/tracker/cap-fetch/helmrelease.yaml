apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tracker-cap-fetch
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: prd-tracker
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: prd
    module: tracker
    app: cap-fetch
    replicas: 1
    service:
      targetPort: 5050
    resources:
      memory: 4Gi
    ingress:
      enabled: false
    secrets:
      - prd-platform-pcgi-raw
      - prd-module-tracker
    env: []
