apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-573-stops-tts-server
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-573-stops
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-573
    module: stops
    app: tts-server
    replicas: 1
    service:
      targetPort: 8000
    ingress:
      enabled: true
      host: pr-573.go-stg.tmlmobilidade.pt
    secrets:
      - stg-platform-storage
      - stg-platform-sentry-stops
    env: []
  suspend: false
