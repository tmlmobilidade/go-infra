apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pr-553-plans-organizer
  namespace: flux-system
spec:
  interval: 30s
  dependsOn:
    - name: onepassword-connect
  targetNamespace: pr-553-plans
  install:
    createNamespace: true
  chart:
    spec:
      chart: compute/kubernetes/charts/go-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  values:
    environment: pr-553
    module: plans
    app: organizer
    replicas: 1
    service:
      targetPort: 5050
    ingress:
      enabled: true
      host: pr-553.go-stg.tmlmobilidade.pt
    secrets:
      - stg-platform-go-redis
      - stg-platform-storage
      - stg-module-plans
    env: []
  suspend: false
